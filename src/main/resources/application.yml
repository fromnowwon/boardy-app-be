spring:
  config:
    import: "optional:dotenv:.env"
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local} # 개발용 (추후 prod로 변경)
  application:
    name: boardy
  jackson:
    serialization:
      INDENT_OUTPUT: true

jwt:
  secret: ${JWT_SECRET}

server:
  port: 8080
